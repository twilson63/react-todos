import React from 'react'

import CheckBox from '../components/checkbox'

const ToggleAll = props => {
  return <CheckBox className="toggle-all">Mark all as complete</CheckBox>
}

export default ToggleAll
